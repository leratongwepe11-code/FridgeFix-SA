name: fridge_fix_sa
description: A new Flutter project for finding recipes based on fridge ingredients.
publish_to: 'none'
version: 1.0.0+1

environment:
  sdk: '>=3.0.0 <4.0.0'

dependencies:
  flutter:
    sdk: flutter
  url_launcher: ^6.2.5

flutter:
  uses-material-design: true
